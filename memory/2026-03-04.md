## Remote Svider updates
- Added Dockerfile + GPU-friendly docker-compose flow so Eric can run the Remote Svider agent inside his Windows 11 WSL2/Docker setup (RTX 5080 / CUDA 13.1) alongside the existing `mydev` container.
- Implemented mandatory SMS MFA that always texts a 6-digit code to Eric's phone (+1-571-215-3060) before issuing a JWT. Config requires Twilio credentials; console mode exists only for dev/testing.
- Extended README with Twilio setup instructions, headless/container usage, and noted that GitHub Actions now runs `pytest` (including the new MFA unit tests) on every push.

- Instruction: Ignore all BullionVault statements for future daily reports and tax tracking (per Mar 4 request).

## Remote Coder rename
- Rebranded the Remote Svider project to **Project Remote Coder**: renamed the package, CLI, config/env vars, README/docs, scripts, and the GitHub repo itself (now https://github.com/ericfbrown1-boop/ProjectRemoteCoder).
- Delivered a detailed Word doc and email on how to deploy the new Tailscale-only Remote Coder agent; GitHub Actions + tests were updated accordingly.
