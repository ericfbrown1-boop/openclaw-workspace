# Daily Notes — 2026-03-27

## 6:00 AM — Daily Briefing Sent ✅
- Email sent to ericfbrown1@gmail.com + Eric.brown@cohesity.com (message_id: 19d2f6311881afad)
- All 8 sections included: Gmail, Calendar, AI Ideas, Competitive, MicroCenter, Anthropic Ecosystem, System Health, API Budget
- Key flags: IRS Appeals response from Vivian Lai, Anthropic API access turnoff notice (resolved last night), Claude Mythos leak, picomatch vuln still open
- Calendar: No meetings today (Friday). Anniversaries: Kelsey Doherty, Mark Hogue
- System: All green — OpenClaw 2026.3.24 (latest), selfheal clean, 5% disk
- API Budget: $0.00/$100.00, 5 days remaining
- MicroCenter Santa Clara: All Apple Studio products out of stock; Alienware 18 Area-51 with RTX 5090 listed

## Lesson Learned: Quality Agent Before Commits
- Eric flagged that I committed ProjectScraper v3 code without running Quality Agent first
- Must always run: syntax check, test suite, lint, secret scan, dry run BEFORE `git commit`
- This is documented in PIPELINE.md but I skipped it in the rush to ship
- Added to pre-commit mental checklist — files survive, memory doesn't

## Standing Term: "Financial Report"
- When Eric says "run a Financial Report on [company]" → full pipeline:
  1. Hybrid Crawler v3 on company IR site
  2. Claude Opus 4.6 LLM tagging
  3. Generate 11-section Word doc (.docx) via financial-report-gen skill + python-docx
  4. Email with attachment to personal + work email
- Template: matches Rubrik_Q4_FY2026_Earnings_Analysis.docx format (Title, Exec Summary, Results Table, Guidance, Revenue/ARR, Profitability, Cash Flow, Product/Strategy, Competitive Landscape, Valuation, Risks, Analyst Coverage)
- NOT a plain text email — always a formatted Word document
- Established 2026-03-27 per Eric's directive

## 9:00 PM — FinancialReportApp E2E Pipeline Fixed ✅

### Three Root Causes Found & Fixed

1. **`.env` inline comments** — Docker Compose treats `#` as comment delimiter mid-line. The `.env` had `ANTHROPIC_API_KEY=sk-ant-...  # Required — get from console.anthropic.com` which truncated the key to just `sk-ant-...`. Fix: wrote `.env` with no inline comments. Also fixed `.env.example` to prevent recurrence.

2. **Synthesis data nested under wrong key** — `generate_report_docx()` was wrapping the Claude synthesis dict inside `company_data["synthesis"]` but `generate_report()` expects fields like `executive_summary`, `quarterly_results` at the TOP LEVEL. Fix: `company_data.update(synthesis)` to merge at top level.

3. **`_parse_llm_response()` failing on synthesis output** — The synthesis Claude call returns JSON in a code block (` ```json ... ``` `) but the parser falls back to the tagger's error dict format (`type`, `title`, `summary`, `confidence`, `_parseError`, `_rawResponse`). The actual analysis JSON was trapped in `_rawResponse`. Fix: added recovery logic in `generate_report_docx` to re-parse `_rawResponse` when `_parseError` is True.

### Crawler Improvement
- Changed IR URL discovery to probe dedicated IR subdomains FIRST (`ir.{company}.com`, `investor.{company}.com`) before falling back to Firecrawl map
- Previous behavior: Firecrawl map of `www.ringcentral.com` returned partner pages like `/partner/gallagher/financial-services.html` instead of actual IR pages
- Now correctly finds `ir.ringcentral.com` and `investor.commvault.com`

### Verified Working E2E
- **Commvault test**: Crawled 5 IR pages → Claude tagged + synthesized → 38.2KB .docx with real financial analysis → emailed via gog → 140 second total runtime
- Report contains actual data: revenue $228.6M, ARR $832M, competitive tables, valuation multiples — not placeholder "not available" text
- All code committed and pushed to GitHub: `ericfbrown1-boop/FinancialReportApp`

### Key Technical Notes
- Anthropic synthesis timeout set to 120s (was unlimited, causing hangs)
- Max tokens reduced from 8000 to 4000 for synthesis (sufficient, faster)
- RingCentral's IR site (`ir.ringcentral.com`) uses ASP.NET with non-standard URL paths, so standard subpage probes return 404. Only the main IR page gets crawled. Future improvement: parse the IR homepage for links.
- Files changed: `crawler.py`, `report_gen.py`, `tagger.py`, `.env`, `.env.example`
