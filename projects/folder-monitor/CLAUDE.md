# Folder Monitor — Claude Code Instructions

## What This Is
Python file system monitor that watches directories for changes.

## Build & Run
```bash
pip install -r requirements.txt
python monitor.py
```

## Testing
- `python -c "import monitor"` for import check
- Verify: monitor detects file creation in watched directory
